using System.Collections.Generic;
using System.Numerics;
using BulletHell;
using BulletHell.GameEngine;

namespace BulletHell.ObjectCreation {
    public class EnemyFactory
    {
        public EnemyFactory() {}
        public Enemy makeEnemy(string texture, int health, Vector2 startingLocation, List<PathData> pathData, string gun)
        {
            return null;
        }
    }
}